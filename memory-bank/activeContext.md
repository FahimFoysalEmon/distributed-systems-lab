# Active context

## Current focus

Project scaffolding complete. Ready to start **consistent hashing** as the first lab topic.

## Recent changes

- Created Spring Boot 4.1.1 + Maven project
- Added actuator, webmvc, validation
- Added `GET /api/lab` info endpoint
- Added memory-bank + README learning roadmap
- Docker deferred by design

## Next steps

1. Implement consistent hashing (ring, virtual nodes, rebalance demo)
2. Add unit tests showing key remapping when a node is added/removed
3. Later: caching → Kafka (with Docker) → rate limiting

## Preferences

- Maven (not Gradle)
- Spring Boot first; Docker later
- Use this repo to learn system design hands-on
